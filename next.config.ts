import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: [
    "allen-explanation-provider-writings.trycloudflare.com",
    "neil-protected-sit-sophisticated.trycloudflare.com",
  ],
};

export default nextConfig;
